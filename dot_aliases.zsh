# vanilla
alias cl="clear"
alias cls="cl; ls"
alias cppwd="pwd && pwd | pbcopy"
alias cpwd="pwd | pbcopy"
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias lh='ls -lh'
alias clh="cl; lh"

# git
alias g="git"
alias wkt="git-fzf worktree list"

# neovim
alias nv="nvim"

# python
alias ipy="ipython"
alias pipgrep="pip list | grep -i"

# docker
alias compose-run="docker compose run --rm"
